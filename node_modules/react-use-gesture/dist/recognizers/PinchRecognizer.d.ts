import { TouchEvent } from 'react';
import DistanceAngleRecognizer from './DistanceAngleRecognizer';
import GestureController from '../controllers/GestureController';
import { TransformedEvent, ReactEventHandlerKey, Fn } from '../types';
export default class PinchRecognizer extends DistanceAngleRecognizer {
    constructor(controller: GestureController, args: any[]);
    onStart: (event: TransformedEvent<TouchEvent<Element>>) => void;
    onChange: (event: TransformedEvent<TouchEvent<Element>>) => void;
    onEnd: (event: TransformedEvent<TouchEvent<Element>>) => void;
    onCancel: (event: TransformedEvent<TouchEvent<Element>>) => void;
    getEventBindings(): [ReactEventHandlerKey | ReactEventHandlerKey[], Fn][];
}
