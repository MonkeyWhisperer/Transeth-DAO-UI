/// <reference types="react" />
import Recognizer from './Recognizer';
import { DistanceAngle, GestureState, TransformedEvent } from '../types';
/**
 * Abstract class for distance/angle-based gesture recongizers
 */
export default abstract class DistanceAngleRecognizer extends Recognizer<DistanceAngle> {
    /**
     * Utility function to get kinematics of the gesture
     * @d distance
     * @a angle
     * @event
     * @returns set of values including delta, velocities, turns
     */
    protected getKinematics: ([d, a]: [number, (number | undefined)?], event: TransformedEvent<import("react").MouseEvent<Element, MouseEvent> | import("react").TouchEvent<Element> | import("react").WheelEvent<Element> | import("react").PointerEvent<Element> | import("../types").GestureEvent>) => Partial<GestureState<DistanceAngle>>;
}
